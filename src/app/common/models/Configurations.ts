export interface Config {
      id: string;
      name: string;
      case: string;
      cpu_brand: string;
      cpu: string;
      mobo: string;
      ram: string;
      gpu: string;
      cpu_cooler: string;
      psu: string;
      ssd: string;
      hdd: string;
      createTime: Date;
}