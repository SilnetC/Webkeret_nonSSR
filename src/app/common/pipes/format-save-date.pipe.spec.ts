import { FormatSaveDatePipe } from './format-save-date.pipe';

describe('FormatSaveDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSaveDatePipe();
    expect(pipe).toBeTruthy();
  });
});
